// Dino systems (Stage 2). Stage 1 ships this as a stub so the tick loop and
// protocol are already shaped for it.

export function updateDinos(dt, now) {}

export function wireDinos() { return []; }

export function attack(p, m) {}

export function feed(p, m) {}

export function dinoCmd(p, m) {}
